"""Audience-embedding evaluation harness.

Per E2-3 in Epic 2 (`docs/proposals/AUDIENCE_PLANNER_3TYPE_EXTENSION_2026-04-25.md`
§6.5 row E2-3). Compares the embedding generated by each `EMBEDDING_MODE`
on a fixed corpus of campaign briefs and reports per-mode quality metrics
(self-similarity, distinctiveness, dim).

The harness runs offline (no seller dependency) — it just exercises
`UCPClient.create_query_embedding_with_provenance()` across modes and
emits a structured report. Used by §17 release-gate audits and to inform
E2-4's threshold recalibration.
"""

from .embedding_eval import (
    EMBEDDING_EVAL_FIXTURES,
    EvalReport,
    PerModeMetrics,
    evaluate_embedding_modes,
)

__all__ = [
    "EMBEDDING_EVAL_FIXTURES",
    "EvalReport",
    "PerModeMetrics",
    "evaluate_embedding_modes",
]
