# Requirements for the Strands + A2A AgentCore runtime (a2a_main.py).
# Adaptation for Strands + A2A — see ADAPTATION.md. Deliberately EXCLUDES
# crewai/crewai-tools: the A2A runtime path does not import them (AC-5).
# The legacy CrewAI path keeps using requirements.txt.
bedrock-agentcore
strands-agents[a2a]
strands-agents-tools
pydantic>=2.0.0
pydantic-settings>=2.0.0
httpx>=0.27.0
mcp>=1.0.0
python-dotenv>=1.0.0
fastapi>=0.115.0
uvicorn>=0.30.0
aiosqlite>=0.20.0
asyncpg>=0.30.0
redis>=5.0.0
boto3>=1.34.0
