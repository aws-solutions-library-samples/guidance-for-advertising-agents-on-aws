bedrock-agentcore==1.18.1
# NO adcp here, and it is not an oversight. `adcp==6.6.0` requires a2a-sdk>=1.0.1,<1.0.2 while
# `strands-agents[a2a]==1.48.0` requires a2a-sdk>=0.3.0,<0.4.0 — mutually exclusive, and uv refuses the
# resolution outright ("your requirements are unsatisfiable"). So this runtime cannot carry the AdCP SDK
# until one of those two pins moves, which is why the buyer hand-builds its request payloads while every
# seller and the governance agent construct SDK types.
#
# Adding it anyway builds nothing and deploys nothing: the image fails at `uv pip install` and the
# previous image keeps serving, so a deploy can look merely slow rather than broken.
strands-agents[a2a]==1.48.0
mcp==1.28.1
python-dotenv==1.2.2
boto3>=1.34.0
PyJWT==2.13.0
requests>=2.32.0
httpx==0.28.1
